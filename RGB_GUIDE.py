"""
简化版测试指南 - 仅使用RGB，不使用深度
"""

print("""
╔══════════════════════════════════════════════════════════════════╗
║                                                                  ║
║     RealSense 仅RGB模式测试指南（不使用深度）                     ║
║                                                                  ║
╚══════════════════════════════════════════════════════════════════╝

🎯 你的选择：只用RGB彩色图像，不用深度信息

优势：
  ✅ 更简单，只需要彩色图像
  ✅ 符合作业要求（基于OpenCV的图像处理）
  ✅ RealSense仍然提供准确的相机内参（无需手动标定）
  ✅ 通过PnP算法估计位姿

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 三步测试流程（非常简单！）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第1步：测试RealSense相机
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

命令：
    python test_realsense.py

目的：
    - 确认相机正常工作
    - 查看相机内参信息（会自动应用到程序中）

操作：
    - 看到窗口显示图像就成功了
    - 按 'q' 退出

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第2步：调整HSV阈值（最关键！）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

命令：
    python hsv_tuner_realsense.py

这是最重要的一步！必须找到正确的HSV值才能检测到灯带。

操作步骤：
    1. 把灯带放在相机前（距离0.5-1米）
    2. 观察3个窗口：
       - Original + Contours：原始图像，会显示检测到的轮廓
       - Mask：黑白图像，白色=检测到的区域
       - Result：过滤后的彩色结果

    3. 调整滑动条，目标是：
       ✅ Mask窗口中，只有灯带是白色
       ✅ 其他地方都是黑色
       ✅ Original窗口显示检测到的轮廓数量

    4. 提示：
       - 按 'c' 切换颜色预设（红/蓝/绿）
       - 从预设开始微调更容易

    5. 按 'q' 退出，程序会输出HSV值

重要：记录输出的6个值！
    H_min, H_max, S_min, S_max, V_min, V_max

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第3步：配置参数
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

编辑 config.py，只需要改2个地方：

1. ✅ 灯带物理尺寸（用尺子测量）

找到这几行：
    LIGHT_LENGTH = 0.20  # 改成你的灯带长度（单位：米）
    LIGHT_WIDTH = 0.03   # 改成你的灯带宽度（单位：米）

例如：
    - 灯带长20厘米 → 0.20
    - 灯带宽3厘米 → 0.03

2. ✅ HSV阈值（从第2步获得）

找到这几行：
    HSV_LOWER_RED1 = np.array([0, 100, 100])
    HSV_UPPER_RED1 = np.array([10, 255, 255])

改成：
    HSV_LOWER_RED1 = np.array([H_min, S_min, V_min])
    HSV_UPPER_RED1 = np.array([H_max, S_max, V_max])

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第4步：运行主程序
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

命令：
    python main_rgb.py

这就是最终程序！

程序会实时显示：
    ✅ 检测到的灯带数量
    ✅ 绿色矩形框（检测到的灯带）
    ✅ 红色矩形框（旋转矩形拟合）
    ✅ 4个角点 P1, P2, P3, P4（红色圆点）
    ✅ 坐标轴（X红、Y绿、Z蓝）
    ✅ 当前位置（X, Y, Z）单位：米
    ✅ 位移（dX, dY, dZ, Distance）单位：米
    ✅ 姿态角度（Roll, Pitch, Yaw）单位：度

操作：
    - 按 'q'：退出程序
    - 按 's'：保存当前帧到 results/ 目录
    - 按 'r'：重置初始位置

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 快速测试（推荐）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

如果你想快速看效果，可以先用默认参数：

1. python hsv_tuner_realsense.py    # 调整HSV（必须做！）
2. 编辑 config.py                   # 填入HSV值和灯带尺寸
3. python main_rgb.py               # 运行！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 为什么不用深度？
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

作业要求是"基于OpenCV的灯带识别与目标位移解算"：

✅ 只用RGB完全满足要求
✅ 展示了完整的计算机视觉流程：
   - 图像处理（HSV阈值）
   - 特征提取（轮廓、角点）
   - 几何建模（3D模型）
   - 位姿估计（PnP算法）
   - 位移计算

✅ RealSense的优势仍然保留：
   - 自动提供准确的相机内参
   - 无需手动相机标定
   - 高质量的RGB图像

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ 重要提示
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. HSV阈值是关键！
   - 必须花时间调整好
   - 确保只检测到灯带
   - 检测不到灯带 = HSV值不对

2. 灯带尺寸要准确：
   - 用尺子仔细测量
   - 单位必须是米（m）
   - 尺寸误差会影响位姿精度

3. 光照条件：
   - 避免强光直射
   - 避免太暗
   - 尽量保持稳定光照

4. 灯带要平整：
   - 不要弯曲
   - 角点检测需要清晰的边缘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 现在开始测试！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第一步：运行HSV调试工具
    python hsv_tuner_realsense.py

把灯带放在相机前，调整滑动条！

调好后，填入 config.py，然后运行：
    python main_rgb.py

祝你完成作业顺利！🎉

""")
