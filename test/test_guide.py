"""
完整测试指南 - 从相机测试到灯带识别
"""

# ============================================================
#  第一步：测试相机是否正常工作
# ============================================================

print("""
╔════════════════════════════════════════════════════════════╗
║              第一步：测试相机                               ║
╚════════════════════════════════════════════════════════════╝

运行命令：
    python -m test.test_camera

预期结果：
    ✅ 能看到相机画面
    ✅ 显示分辨率信息
    ✅ 按 's' 可以保存图像
    ✅ 按 'q' 可以退出

如果失败：
    ❌ 检查相机是否被其他程序占用
    ❌ 尝试修改 VideoCapture(0) 为 VideoCapture(1)
    ❌ 检查相机权限

""")

# ============================================================
#  第二步：测试HSV颜色检测（找到合适的阈值）
# ============================================================

print("""
╔════════════════════════════════════════════════════════════╗
║          第二步：调整HSV颜色阈值                            ║
╚════════════════════════════════════════════════════════════╝

运行命令：
    python -m test.hsv_tuner

这个工具会：
    ✅ 显示实时相机画面
    ✅ 提供6个滑动条调整HSV阈值
    ✅ 实时显示检测到的灯带区域
    ✅ 输出最佳HSV值，供config.py使用

操作方法：
    1. 把灯带放在相机前
    2. 调整6个滑动条，直到只检测到灯带（白色区域）
    3. 记录下H_min, H_max, S_min, S_max, V_min, V_max的值
    4. 把这些值填入 config.py

""")

# ============================================================
#  第三步：相机标定（获取内参和畸变系数）
# ============================================================

print("""
╔════════════════════════════════════════════════════════════╗
║          第三步：相机标定                                    ║
╚════════════════════════════════════════════════════════════╝

⚠️  这一步很重要！没有标定数据，位姿估计会不准确

准备工作：
    1. 打印一个棋盘格标定板（推荐9x6内角点）
       - 打印在A4纸上
       - 尽量平整，可以贴在硬板上

    2. 测量每个方格的边长（单位：米）
       - 例如：2.5cm = 0.025m

运行命令：
    python -m test.calibrate_camera

操作步骤：
    1. 程序会打开相机
    2. 把棋盘格放在相机前不同位置和角度
    3. 当检测到棋盘格时，按空格键拍摄
    4. 至少拍摄10-15张图片（角度越多样越好）
    5. 按ESC键结束，程序自动进行标定
    6. 标定结果保存在 calibration_result.txt

拍摄技巧：
    ✅ 距离：近、中、远
    ✅ 角度：正面、左倾、右倾、上倾、下倾
    ✅ 位置：左上、右上、中间、左下、右下

标定完成后：
    将 calibration_result.txt 中的参数复制到 config.py

""")

# ============================================================
#  第四步：配置参数
# ============================================================

print("""
╔════════════════════════════════════════════════════════════╗
║          第四步：配置参数                                    ║
╚════════════════════════════════════════════════════════════╝

编辑 config.py，填入以下参数：

1. 相机内参矩阵（从标定结果获得）：
   CAMERA_MATRIX = np.array([
       [fx, 0, cx],
       [0, fy, cy],
       [0, 0, 1]
   ], dtype=np.float32)

2. 畸变系数（从标定结果获得）：
   DIST_COEFFS = np.array([k1, k2, p1, p2, k3], dtype=np.float32)

3. 灯带物理尺寸（用尺子测量）：
   LIGHT_LENGTH = 0.20  # 长度，单位：米
   LIGHT_WIDTH = 0.03   # 宽度，单位：米

4. HSV颜色阈值（从HSV调试工具获得）：
   HSV_LOWER_RED1 = np.array([H_min, S_min, V_min])
   HSV_UPPER_RED1 = np.array([H_max, S_max, V_max])

""")

# ============================================================
#  第五步：运行完整程序
# ============================================================

print("""
╔════════════════════════════════════════════════════════════╗
║          第五步：运行完整程序                                ║
╚════════════════════════════════════════════════════════════╝

运行命令：
    python main_rgb.py

程序会显示：
    ✅ 实时相机画面
    ✅ 检测到的灯带（绿色框）
    ✅ 旋转矩形（红色框）
    ✅ 4个角点（P1-P4）
    ✅ 坐标轴（X红、Y绿、Z蓝）
    ✅ 位置信息（X, Y, Z）
    ✅ 位移信息（dX, dY, dZ, Distance）

操作：
    - 按 'q'：退出
    - 按 's'：保存当前帧
    - 按 'r'：重置初始位置

""")

# ============================================================
#  快速测试（跳过标定）
# ============================================================

print("""
╔════════════════════════════════════════════════════════════╗
║          快速测试模式（如果没有标定板）                      ║
╚════════════════════════════════════════════════════════════╝

如果暂时没有标定板，可以先用默认参数测试检测功能：

1. 先测试HSV颜色检测：
   python -m test.hsv_tuner

   调整参数直到能检测到灯带

2. 更新config.py中的HSV阈值

3. 运行主程序（位姿不准确，但能看到检测效果）：
   python main_rgb.py

注意：
   ⚠️  没有正确的相机标定，位姿估计（X,Y,Z）会不准确
   ⚠️  但是可以看到检测、矩形拟合、角点提取的效果

""")

# ============================================================
#  常见问题
# ============================================================

print("""
╔════════════════════════════════════════════════════════════╗
║          常见问题                                            ║
╚════════════════════════════════════════════════════════════╝

Q1: 检测不到灯带？
A1:
   - 调整HSV阈值（使用hsv_tuner.py）
   - 检查光照条件（避免太暗或太亮）
   - 调整config.py中的MIN_CONTOUR_AREA和长宽比参数

Q2: 相机打不开？
A2:
   - 检查相机权限
   - 关闭其他占用相机的程序
   - 尝试不同的相机索引（0, 1, 2）

Q3: 位姿不准确？
A3:
   - 必须进行相机标定
   - 准确测量灯带物理尺寸
   - 确保角点匹配正确

Q4: 程序卡住或崩溃？
A4:
   - 检查OpenCV版本
   - 更新显卡驱动
   - 尝试降低图像分辨率

""")

print("""
╔════════════════════════════════════════════════════════════╗
║          测试流程总结                                        ║
╚════════════════════════════════════════════════════════════╝

推荐测试顺序：

1. ✅ python -m test.test_camera     # 测试相机
2. ✅ python -m test.hsv_tuner       # 调整HSV阈值
3. ✅ python -m test.calibrate_camera # 相机标定（需要棋盘格）
4. ✅ 编辑 config.py                # 填入参数
5. ✅ python main_rgb.py            # 运行完整程序

祝你测试顺利！🎉
""")
