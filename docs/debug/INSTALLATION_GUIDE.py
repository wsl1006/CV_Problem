"""
D435i 灯带检测系统 - 安装和运行指南
"""

print("""
╔══════════════════════════════════════════════════════════════════╗
║                                                                  ║
║     Intel RealSense D435i 灯带检测系统 - 完整安装指南            ║
║                                                                  ║
╚══════════════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📦 1. 安装依赖包 (Ubuntu 22.04)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 1.1 安装 Python 依赖
pip install opencv-python numpy pyrealsense2

# 如果 pip 安装 pyrealsense2 失败，使用 conda:
conda install -c conda-forge pyrealsense2

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔌 2. 验证 D435i 连接

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 2.1 检查 USB 设备
lsusb | grep Intel

# 应该看到类似：
# Bus 002 Device 003: ID 8086:0b3a Intel Corp. RealSense D435i

# 2.2 检查视频设备
ls /dev/video*

# 应该看到多个 /dev/video0, /dev/video1, ...

# 2.3 测试 RealSense Viewer（如果已安装）
realsense-viewer

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 3. 运行程序

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

cd /home/batman/CV_Problem

python3 main_rgb.py

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 4. 判断系统是否正常工作

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4.1 D435i 连接成功的标志：

启动时终端显示：

    ✅ Intel RealSense D435i 已成功启动

    RGB Camera:
      分辨率: 640 × 480
      帧率: 30 FPS

    Intrinsics (真实D435i RGB内参):
      fx = 614.xxx
      fy = 614.xxx
      cx = 321.xxx
      cy = 238.xxx

    Distortion (畸变系数):
      k1 = 0.xxxxxx
      k2 = 0.xxxxxx
      ...

如果内参值都是真实数字（不是 800.0, 320.0 这种示例值），说明成功！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4.2 RGB图像正常的标志：

窗口标题：
    "Light Tracking (D435i RGB Only)"

能看到实时彩色图像，不是黑屏或灰屏。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4.3 灯带检测成功的标志：

图像中：
    ✅ 绿色矩形框 - 检测到的灯带轮廓
    ✅ P1(TL), P2(TR), P3(BR), P4(BL) - 四个角点标注
    ✅ 红色圆点 - 标注角点位置

左上角显示：
    Detected Lights: 1

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4.4 四角点排序正确的标志：

观察图像中的标注：
    P1(TL) - 应该在灯带左上
    P2(TR) - 应该在灯带右上
    P3(BR) - 应该在灯带右下
    P4(BL) - 应该在灯带左下

如果位置对应正确，说明排序成功！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4.5 PnP成功的标志：

窗口左上角显示：
    Position:
      X = ±x.xxx m
      Y = ±y.xxx m
      Z = +z.xxx m  (这个值应该 > 0，表示灯带在相机前方)

如果 DEBUG=True，终端显示：
    [PnP] 位姿估计成功
      rvec = [...]
      tvec = [...]
      X = xxx m
      Y = xxx m
      Z = xxx m (相机到灯带的距离)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4.6 XYZ结果可信的判断：

✅ Z值合理：
   - 如果灯带离相机 50cm，Z 应该约为 0.5m
   - 如果灯带离相机 1m，Z 应该约为 1.0m

✅ 移动灯带时：
   - 向左移动 → X 变小
   - 向右移动 → X 变大
   - 向上移动 → Y 变小（注意相机坐标系Y向下）
   - 向下移动 → Y 变大
   - 靠近相机 → Z 变小
   - 远离相机 → Z 变大

✅ 坐标轴显示：
   - 红色线（X轴）应该指向灯带长度方向
   - 绿色线（Y轴）应该指向灯带宽度方向
   - 蓝色线（Z轴）应该指向相机方向

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4.7 位移计算正确的标志：

第一次检测到灯带时，终端显示：
    ✅ 已设置初始位置

然后移动灯带：
    Displacement:
      dX = ±x.xxx m
      dY = ±y.xxx m
      dZ = ±z.xxx m
      Distance = x.xxx m

按 'r' 键后，位移重置为 0。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ 5. 常见问题排查

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

5.1 问题：找不到 D435i

错误信息：
    ❌ 无法启动 RealSense D435i

排查：
    1. lsusb | grep Intel  # 检查USB连接
    2. 重新插拔USB线（使用USB 3.0蓝色接口）
    3. 检查其他程序是否占用相机
    4. sudo usermod -a -G video $USER  # 添加权限
    5. 重启电脑

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

5.2 问题：检测不到灯带

显示：
    "No light detected"

排查：
    1. 运行 HSV 调试工具重新标定：
       python3 -m test.hsv_tuner_realsense

    2. 检查 core/config.py 中的 HSV 参数是否正确

    3. 检查光照条件（避免强光、阴影）

    4. 确保灯带在视野中且足够亮

    5. 临时降低 MIN_ASPECT_RATIO（如果灯带不够细长）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

5.3 问题：PnP 失败或 Z 值异常

可能原因：
    1. 灯带尺寸不准确
       → 重新测量 LIGHT_LENGTH 和 LIGHT_WIDTH

    2. 角点排序错误
       → 检查图像中 P1-P4 标注位置

    3. 相机内参读取失败
       → 检查启动时打印的内参值

    4. 灯带严重弯曲或变形
       → 确保灯带平整

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

5.4 问题：Pitch 不是 0

显示：
    Pitch = -64.52 deg

说明：
    相机没有正对灯带，而是俯视或仰视。

解决：
    调整相机角度，让它水平正对灯带。
    目标：Pitch ≈ 0° (±5° 以内)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 6. 开启/关闭调试模式

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

编辑 core/config.py:

    DEBUG = True   # 打印详细信息
    DEBUG = False  # 关闭调试输出

DEBUG = True 时，终端会打印：
    - 检测到的轮廓数量
    - 轮廓面积、长宽比
    - 2D角点坐标
    - 3D模型坐标
    - PnP求解结果
    - rvec, tvec
    - Roll, Pitch, Yaw
    - 位移信息

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 7. 验证系统完整性检查清单

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

□ D435i 已连接（lsusb 可见）
□ pyrealsense2 已安装
□ 启动时显示真实内参（不是示例值）
□ RGB 图像正常显示
□ 检测到灯带（绿色框）
□ 四角点标注位置正确（P1-P4）
□ 坐标轴显示正常（红绿蓝三色）
□ Z 值合理（>0，符合实际距离）
□ 移动灯带时 XYZ 变化正确
□ 位移计算正常
□ 按 'r' 可重置位移

如果以上全部 ✅，系统工作正常！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎉 现在开始测试！

运行：
    python3 main_rgb.py

祝你成功！

""")
