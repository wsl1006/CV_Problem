"""
RealSense 测试指南
"""

print("""
╔══════════════════════════════════════════════════════════════════╗
║                                                                  ║
║        使用 Intel RealSense 相机的测试指南                        ║
║                                                                  ║
╚══════════════════════════════════════════════════════════════════╝

🎉 太好了！你使用的是 RealSense 深度相机！

RealSense 的优势：
  ✅ 自动提供准确的相机内参（无需手动标定！）
  ✅ 提供深度信息（可以直接获取距离）
  ✅ 畸变校正已内置
  ✅ 高质量的RGB图像

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 RealSense 测试流程（比普通相机简单！）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第1步：测试 RealSense 是否正常工作
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

命令：
    python -m test.test_realsense

预期效果：
    ✅ 看到彩色图像窗口
    ✅ 看到深度图像窗口（彩色编码）
    ✅ 移动鼠标可以看到深度值
    ✅ 显示相机内参信息

操作：
    - 移动鼠标：查看深度值
    - 按 's'：保存图像
    - 按 'd'：切换深度显示
    - 按 'q'：退出

如果失败：
    ❌ 检查USB连接（建议使用USB 3.0蓝色接口）
    ❌ 检查是否安装了RealSense驱动
    ❌ 尝试重新插拔相机

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第2步：调整HSV阈值找到灯带颜色
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

命令：
    python -m test.hsv_tuner_realsense

这是最重要的一步！

操作方法：
    1. 把你的灯带放在RealSense相机前
    2. 你会看到3个窗口：
       - Original + Contours：原始图像+检测结果
       - Mask：检测掩码（白色=检测到的区域）
       - Result：过滤后的结果
       - HSV Tuner：6个滑动条

    3. 调整6个滑动条：
       - H Min / H Max：色调范围
       - S Min / S Max：饱和度范围
       - V Min / V Max：亮度范围

    4. 目标：让 Mask 窗口中只显示灯带（白色区域）

    5. 按 'c' 可以快速切换预设值：
       - 红色灯带预设
       - 蓝色灯带预设
       - 绿色灯带预设

    6. 按 'q' 退出，程序会输出最终的HSV值

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第3步：配置参数
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

编辑 core/config.py 文件：

1. ⚠️ 相机内参（RealSense会自动提供，但你也可以手动设置）

   从 test_realsense.py 的输出中复制内参矩阵

2. ✅ 灯带物理尺寸（用尺子测量，单位：米）

   LIGHT_LENGTH = 0.20  # 例如：20厘米 = 0.20米
   LIGHT_WIDTH = 0.03   # 例如：3厘米 = 0.03米

3. ✅ HSV颜色阈值（从 test/hsv_tuner_realsense.py 获得）

   HSV_LOWER_RED1 = np.array([H_min, S_min, V_min])
   HSV_UPPER_RED1 = np.array([H_max, S_max, V_max])

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第4步：运行完整程序
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

命令：
    python -m test.main_realsense

程序会实时显示：
    ✅ 检测到的灯带（绿色框）
    ✅ 旋转矩形（红色框）
    ✅ 4个角点（P1-P4，红色圆点）
    ✅ 坐标轴（X红、Y绿、Z蓝）
    ✅ 每个角点的深度值
    ✅ 位置信息（X, Y, Z）单位：米
    ✅ 位移信息（dX, dY, dZ, Distance）单位：米
    ✅ 姿态角度（Roll, Pitch, Yaw）单位：度

操作：
    - 按 'q'：退出程序
    - 按 's'：保存当前帧到results/目录
    - 按 'r'：重置初始位置（当前位置作为新的参考点）
    - 按 'd'：显示/隐藏深度图

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 快速测试流程（推荐）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. ✅ python -m test.test_realsense          # 测试相机
2. ✅ python -m test.hsv_tuner_realsense     # 调整HSV（最重要！）
3. ✅ 编辑 core/config.py                   # 填入HSV值和灯带尺寸
4. ✅ python main_rgb.py                     # 运行完整程序

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 RealSense 的特殊优势
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 无需相机标定！
   RealSense 会自动提供准确的相机内参

✅ 可以直接获取深度！
   每个角点都会显示实际深度值

✅ 位姿估计更准确！
   内参准确 + 深度信息 = 更好的位姿估计

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ 注意事项
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. USB 连接：
   - 使用 USB 3.0 接口（蓝色）
   - 如果USB线太长，可能影响性能

2. 测量灯带尺寸：
   - 用尺子准确测量长度和宽度
   - 单位必须是米（例如：20cm = 0.20m）

3. 光照条件：
   - RealSense对光照较敏感
   - 避免强光直射
   - 避免反光表面

4. HSV阈值：
   - 这是最关键的参数！
   - 花时间调整到最佳效果
   - 确保 Mask 窗口只显示灯带

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🆘 常见问题
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Q1: 找不到RealSense相机？
A:
   - 检查USB连接（必须是USB 3.0）
   - 运行 lsusb 查看是否识别
   - 重新插拔相机
   - 检查是否有权限问题

Q2: 检测不到灯带？
A:
   - 运行 test/hsv_tuner_realsense.py 重新调整HSV阈值
   - 检查光照条件
   - 确保灯带足够亮
   - 调整 core/config.py 中的 MIN_CONTOUR_AREA

Q3: 位姿抖动？
A:
   - 灯带要尽量平整
   - 角点检测要稳定
   - 可以考虑添加滤波（卡尔曼滤波）

Q4: 深度值不准确？
A:
   - RealSense有效深度范围：0.3m - 3m
   - 太近或太远都会影响精度
   - 检查表面材质（反光表面会影响深度）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📦 文件说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

RealSense专用文件：
  ✅ core/camera_realsense.py     # RealSense相机类
  ✅ test/test_realsense.py       # RealSense测试程序
  ✅ test/hsv_tuner_realsense.py  # HSV调试工具（RealSense）
  ✅ test/main_realsense.py       # 旧版 RealSense 测试入口

通用文件：
  ✅ core/detector.py             # 灯带检测器
  ✅ core/geometry.py             # 几何计算
  ✅ core/pose.py                 # 姿态估计
  ✅ core/config.py               # 配置文件

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 现在开始测试！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

第一步：先测试RealSense相机
    python -m test.test_realsense

看到相机画面后，按 'q' 退出，然后：

第二步：调整HSV阈值
    python -m test.hsv_tuner_realsense

调好参数后，就可以运行主程序了：
    python main_rgb.py

祝你测试顺利！🎉

""")
